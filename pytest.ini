[pytest]
# Our Pydantic model is named TestCase; tell pytest it is not a test class.
python_classes = Test*Case
addopts = -q
testpaths = tests
# Ensure the repo root is importable so `import llmqa` works in CI too.
pythonpath = .
filterwarnings =
    ignore::pytest.PytestCollectionWarning
