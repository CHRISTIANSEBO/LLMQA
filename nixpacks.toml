# Nixpacks build config for Railway.
# Let nixpacks' native Python provider handle pip/venv setup (it detects
# requirements.txt automatically); we only pin the start command.
[start]
cmd = "python server.py"
