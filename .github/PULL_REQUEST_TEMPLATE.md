## What & why

Describe what this PR changes and the motivation.

Closes #

## Checklist

- [ ] Branched off `main`.
- [ ] `pytest tests/ -v` passes.
- [ ] `python cli.py run --provider mock --min-pass-rate 0.8 --no-store` passes.
- [ ] New behavior has tests.
- [ ] `README.md` updated if usage / metrics / providers / CLI changed.
- [ ] No secrets, `.env`, or `*.db` files staged.

## Notes for reviewers

Anything worth calling out — trade-offs, follow-ups, screenshots for UI changes.
